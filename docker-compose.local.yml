# Publish the site on localhost:8080. API is same-origin via nginx (/api).
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up --build

services:
  web:
    ports:
      - "8080:80"
